export type FormData = {
    fullname: string
    age: number
    email: string
}

export type PokemonObj = {
    name: string
    url: string
}

export type AbilityObj = {
    ability: { name: string; url: string }
    is_hidden: boolean
    slot: number
}
